{
  "name": "itau-uy-utils",
  "version": "1.3.7",
  "description": "Calculates totals for Itaú credit card transactions",
  "author": "Avi Tretiak",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prepare": "husky",
    "dev": "rollup -wc",
    "clean": "del-cli dist",
    "lint": "eslint --ext .ts . && prettier --ignore-path .eslintignore --check .",
    "lint:fix": "eslint --ext .ts --fix . && prettier --ignore-path .eslintignore --write .",
    "ci": "run-s lint",
    "build:js": "rollup -c",
    "build": "cross-env NODE_ENV=production run-s ci clean build:js"
  },
  "devDependencies": {
    "@gera2ld/plaid": "~2.7.0",
    "@gera2ld/plaid-rollup": "~2.7.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@violentmonkey/types": "^0.1.7",
    "cross-env": "^7.0.3",
    "del-cli": "^5.1.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5",
    "rollup": "^4.0.0",
    "rollup-plugin-typescript2": "^0.36.0",
    "rollup-plugin-userscript": "^0.3.2",
    "typescript": "^5.0.0"
  },
  "lint-staged": {
    "src/**": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}